# 06 Testing Rules

## Rule: Verify the changed slice

Add or update tests around the behavior that changed, not the whole codebase.

## Rule: Prefer the cheapest useful check

Run the narrowest test or validation command that can confirm the fix.